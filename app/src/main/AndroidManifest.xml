<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blabify"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.Folders"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.Registration"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.Autorization"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.Chat"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.Onboarding"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.MainScreen"
            android:exported="false" /> <!-- Splash — главный экран -->
        <activity
            android:name=".presentation.ui.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Остальные экраны -->
        <activity
            android:name=".presentation.ui.ChatsActivity"
            android:exported="false" />
    </application>

</manifest>